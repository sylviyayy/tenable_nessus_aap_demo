# Placeholder — export from AAP or maintain with aap_configuration collection

# Workflow: Demo-Windows-Patch-Lifecycle
# - snapshot.yml → patch_windows.yml → nessus_validate.yml → servicenow_update.yml
# - Optional approval node before patch
