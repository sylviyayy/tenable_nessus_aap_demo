---
# TODO: Replace placeholders after terraform apply (Phase 2).
# Groups must match workflow job template limits in AAP.

all:
  children:
    aws_windows:
      hosts:
        demo-win-aws-01:
          ansible_host: 10.0.1.11  # TODO: EC2 private IP
          cloud: aws
    azure_windows:
      hosts:
        demo-win-az-01:
          ansible_host: 10.1.1.11  # TODO: Azure VM private IP
          cloud: azure
  vars:
    ansible_connection: winrm
    ansible_winrm_transport: ntlm
    ansible_winrm_server_cert_validation: ignore
    # Credentials: use --ask-pass or AAP credential; do not store passwords here.
