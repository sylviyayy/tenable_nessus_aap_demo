[defaults]
inventory = inventories/demo/hosts.yml
roles_path = roles
collections_paths = ~/.ansible/collections
host_key_checking = False
retry_files_enabled = False
stdout_callback = yaml

# Run from repo: cd ansible && ansible-playbook playbooks/site.yml --syntax-check
